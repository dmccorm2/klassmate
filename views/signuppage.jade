extends layout

block content
  div.loginbox
    form(name="login", action="/signup", method="post")
      input(id="user", type="text", name="user", placeholder="username")
      input(id="user", type="password", name="pass", placeholder="password")
      input(type="submit", value="login")